<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <!-- No permissions or AMap apikey declared here: the host app owns those, so the
       merged manifest stays free of duplicate declarations. (R5) -->
</manifest>
