/**
 * This code was generated by "react-native codegen-harmony"
 * 
 * Do not edit this file as changes may cause incorrect behavior and will be
 * lost once the code is regenerated.
 * 
 * @generatorVersion: 1
 */

import {
  Descriptor as ComponentDescriptor,
  ViewBaseProps,
  ViewRawProps,
  ViewDescriptorWrapperBase,
  ColorValue,
  Color,
  RNInstance,
  Tag,
  RNComponentCommandReceiver,
  ViewPropsSelector,
} from '@rnoh/react-native-openharmony/ts';


export namespace RNMapsMapView {
  export const NAME = "RNMapsMapView" as const

  export interface DirectRawProps {
    provider?: string;
    coordinateSystem?: string;
    initialRegion?: {latitude: number, longitude: number, latitudeDelta: number, longitudeDelta: number};
    region?: {latitude: number, longitude: number, latitudeDelta: number, longitudeDelta: number};
    initialCamera?: {latitude: number, longitude: number, heading: number, pitch: number, zoom: number, altitude: number};
    camera?: {latitude: number, longitude: number, heading: number, pitch: number, zoom: number, altitude: number};
    mapType?: string;
    customMapStyle?: string;
    userInterfaceStyle?: string;
    tintColor?: ColorValue;
    mapPadding?: {top: number, right: number, bottom: number, left: number};
    kmlSrc?: string;
    minZoomLevel?: number;
    maxZoomLevel?: number;
    zoomEnabled?: boolean;
    zoomTapEnabled?: boolean;
    zoomControlEnabled?: boolean;
    scrollEnabled?: boolean;
    scrollDuringRotateOrZoomEnabled?: boolean;
    rotateEnabled?: boolean;
    pitchEnabled?: boolean;
    showsUserLocation?: boolean;
    showsMyLocationButton?: boolean;
    showsCompass?: boolean;
    showsScale?: boolean;
    showsTraffic?: boolean;
    showsBuildings?: boolean;
    showsIndoors?: boolean;
    showsIndoorLevelPicker?: boolean;
    showsPointsOfInterest?: boolean;
    loadingEnabled?: boolean;
    loadingIndicatorColor?: ColorValue;
    loadingBackgroundColor?: ColorValue;
    toolbarEnabled?: boolean;
    liteMode?: boolean;
    cacheEnabled?: boolean;
  }
  
  export interface Props extends ViewBaseProps {}
  
  export interface State {}
  
  export interface RawProps extends ViewRawProps, DirectRawProps {}
  
  export class PropsSelector extends ViewPropsSelector<Props, RawProps> {
    get provider() {
      return this.rawProps.provider ?? 'amap';
    }
    
    get coordinateSystem() {
      return this.rawProps.coordinateSystem ?? 'gcj02';
    }
    
    get initialRegion() {
      return this.rawProps.initialRegion;
    }
    
    get region() {
      return this.rawProps.region;
    }
    
    get initialCamera() {
      return this.rawProps.initialCamera;
    }
    
    get camera() {
      return this.rawProps.camera;
    }
    
    get mapType() {
      return this.rawProps.mapType ?? 'standard';
    }
    
    get customMapStyle() {
      return this.rawProps.customMapStyle;
    }
    
    get userInterfaceStyle() {
      return this.rawProps.userInterfaceStyle ?? 'system';
    }
    
    get mapPadding() {
      return this.rawProps.mapPadding;
    }
    
    get kmlSrc() {
      return this.rawProps.kmlSrc;
    }
    
    get minZoomLevel() {
      return this.rawProps.minZoomLevel ?? 3;
    }
    
    get maxZoomLevel() {
      return this.rawProps.maxZoomLevel ?? 20;
    }
    
    get zoomEnabled() {
      return this.rawProps.zoomEnabled ?? true;
    }
    
    get zoomTapEnabled() {
      return this.rawProps.zoomTapEnabled ?? true;
    }
    
    get zoomControlEnabled() {
      return this.rawProps.zoomControlEnabled ?? false;
    }
    
    get scrollEnabled() {
      return this.rawProps.scrollEnabled ?? true;
    }
    
    get scrollDuringRotateOrZoomEnabled() {
      return this.rawProps.scrollDuringRotateOrZoomEnabled ?? true;
    }
    
    get rotateEnabled() {
      return this.rawProps.rotateEnabled ?? true;
    }
    
    get pitchEnabled() {
      return this.rawProps.pitchEnabled ?? true;
    }
    
    get showsUserLocation() {
      return this.rawProps.showsUserLocation ?? false;
    }
    
    get showsMyLocationButton() {
      return this.rawProps.showsMyLocationButton ?? true;
    }
    
    get showsCompass() {
      return this.rawProps.showsCompass ?? true;
    }
    
    get showsScale() {
      return this.rawProps.showsScale ?? false;
    }
    
    get showsTraffic() {
      return this.rawProps.showsTraffic ?? false;
    }
    
    get showsBuildings() {
      return this.rawProps.showsBuildings ?? true;
    }
    
    get showsIndoors() {
      return this.rawProps.showsIndoors ?? true;
    }
    
    get showsIndoorLevelPicker() {
      return this.rawProps.showsIndoorLevelPicker ?? false;
    }
    
    get showsPointsOfInterest() {
      return this.rawProps.showsPointsOfInterest ?? true;
    }
    
    get loadingEnabled() {
      return this.rawProps.loadingEnabled ?? false;
    }
    
    get toolbarEnabled() {
      return this.rawProps.toolbarEnabled ?? true;
    }
    
    get liteMode() {
      return this.rawProps.liteMode ?? false;
    }
    
    get cacheEnabled() {
      return this.rawProps.cacheEnabled ?? false;
    }
    
  
    get tintColor() {
        if (this.rawProps.tintColor) {
          return Color.fromColorValue(this.rawProps.tintColor)
        } else {
          return new Color({ r: 0, g: 0, b: 0, a: 255})
        }
    }
    
    get loadingIndicatorColor() {
        if (this.rawProps.loadingIndicatorColor) {
          return Color.fromColorValue(this.rawProps.loadingIndicatorColor)
        } else {
          return new Color({ r: 0, g: 0, b: 0, a: 255})
        }
    }
    
    get loadingBackgroundColor() {
        if (this.rawProps.loadingBackgroundColor) {
          return Color.fromColorValue(this.rawProps.loadingBackgroundColor)
        } else {
          return new Color({ r: 0, g: 0, b: 0, a: 255})
        }
    }
    
  }

  export type Descriptor = ComponentDescriptor<
    typeof NAME,
    Props,
    State,
    RawProps
  >;
  
  export class DescriptorWrapper extends ViewDescriptorWrapperBase<
    typeof NAME,
    Props,
    State,
    RawProps,
    PropsSelector
  > {
    protected createPropsSelector() {
      return new PropsSelector(this.descriptor.props, this.descriptor.rawProps)
    }
  }
  
  export interface EventPayloadByName {
    "regionChange": {region: {latitude: number, longitude: number, latitudeDelta: number, longitudeDelta: number}, isGesture?: boolean}
    "regionChangeComplete": {region: {latitude: number, longitude: number, latitudeDelta: number, longitudeDelta: number}, isGesture?: boolean}
    "mapReady": {}
    "mapLoaded": {}
    "press": {coordinate: {latitude: number, longitude: number}, position: {x: number, y: number}}
    "longPress": {coordinate: {latitude: number, longitude: number}, position: {x: number, y: number}}
    "doublePress": {coordinate: {latitude: number, longitude: number}, position: {x: number, y: number}}
    "panDrag": {coordinate: {latitude: number, longitude: number}, position: {x: number, y: number}}
    "poiClick": {placeId?: string, name?: string, coordinate: {latitude: number, longitude: number}, position?: {x: number, y: number}}
    "userLocationChange": {coordinate?: {latitude: number, longitude: number, altitude: number, accuracy: number, speed: number, heading: number, isFromMockProvider?: boolean}}
    "commandResult": {id: number, data: string}
  }
  
  export class EventEmitter {
    constructor(private rnInstance: RNInstance, private tag: Tag) {}
    
    emit<TEventName extends keyof EventPayloadByName>(eventName: TEventName, payload: EventPayloadByName[TEventName]) {
      this.rnInstance.emitComponentEvent(this.tag, eventName, payload)
    }
  }
  
  export interface CommandArgvByName {
    "animateToRegion": [number, number, number, number, number]
    "animateCamera": [number, number, number, number, number, number]
    "setCamera": [number, number, number, number, number]
    "fitToCoordinates": [string, string, boolean]
    "fitToElements": [boolean]
    "fitToSuppliedMarkers": [string, string, boolean]
    "getCamera": [number]
    "getMapBoundaries": [number]
    "pointForCoordinate": [number, number, number]
    "coordinateForPoint": [number, number, number]
    "addressForCoordinate": [number, number, number]
    "takeSnapshot": [number, number, number, string, number, string]
    "setMapBoundaries": [number, number, number, number]
    "getMarkersFrames": [number, boolean]
  }
  
  export class CommandReceiver {
    private listenersByCommandName = new Map<string, Set<(...args: any[]) => void>>()
    private cleanUp: (() => void) | undefined = undefined
  
    constructor(private componentCommandReceiver: RNComponentCommandReceiver, private tag: Tag) {
    }
  
    subscribe<TCommandName extends keyof CommandArgvByName>(commandName: TCommandName, listener: (argv: CommandArgvByName[TCommandName]) => void) {
      if (!this.listenersByCommandName.has(commandName)) {
        this.listenersByCommandName.set(commandName, new Set())
      }
      this.listenersByCommandName.get(commandName)!.add(listener)
      const hasRegisteredCommandReceiver = !!this.cleanUp
      if (!hasRegisteredCommandReceiver) {
        this.cleanUp = this.componentCommandReceiver.registerCommandCallback(this.tag, (commandName: string, argv: any[]) => {
          if (this.listenersByCommandName.has(commandName)) {
            const listeners = this.listenersByCommandName.get(commandName)!
            listeners.forEach(listener => {
              listener(argv)
            })
          }
        })
      }
  
      return () => {
        this.listenersByCommandName.get(commandName)?.delete(listener)
        if (this.listenersByCommandName.get(commandName)?.size ?? 0 === 0) {
          this.listenersByCommandName.delete(commandName)
        }
        if (this.listenersByCommandName.size === 0) {
          this.cleanUp?.()
        }
      }
    }
  }

}
