import { AppRegistry } from 'react-native';
import App from './App';

// appKey must match RNApp({ appKey: 'CnMapsExample' }) in entry/.../pages/Index.ets.
AppRegistry.registerComponent('CnMapsExample', () => App);
